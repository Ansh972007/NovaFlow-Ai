# Roadmap

## v4.0 — Enterprise automation (current)

- [x] OCR document pipeline — vision OCR for images in knowledge bases
- [x] LDAP / directory login — enterprise SSO via `LDAP_URL` env
- [x] Workflow cron schedules — automated runs with cron expressions
- [x] Marketplace comments — threaded feedback on public workflows
- [x] Audit log viewer — live activity feed in Settings
- [x] Agent tools — `regex_extract`, `json_parse`, `word_count`

## v3.0 — Governance & community (done)

- [x] Workflow versioning — auto snapshots on save, restore from History tab
- [x] Marketplace ratings — 1–5 stars with community averages
- [x] API keys UI — create/revoke keys in Settings (admin)
- [x] Deploy panel — webhook URL copy + marketplace toggle in workflow builder

## v2.5 — Platform sprint (done)

## Future enhancements

| Feature | Notes |
|---------|--------|
| SAML 2.0 | Enterprise IdP federation |
| Workflow diff view | Compare version snapshots visually |
| Real-time collab | Multi-user workflow editing |

**NovaFlow v4.0** adds OCR ingest, LDAP auth, cron workflow automation, marketplace comments, and audit visibility.
