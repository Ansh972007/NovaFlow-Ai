import WorkflowsClient from "./WorkflowsClient";

export const metadata = {
  title: "Workflows — NovaFlow AI",
  description: "Automate AI pipelines with workflows",
};

export default function WorkflowsPage() {
  return <WorkflowsClient />;
}
