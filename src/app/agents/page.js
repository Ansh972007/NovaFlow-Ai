import AgentsClient from "./AgentsClient";

export const metadata = {
  title: "Agents — NovaFlow AI",
  description: "Run tool-augmented agents with built-in capabilities",
};

export default function AgentsPage() {
  return <AgentsClient />;
}
