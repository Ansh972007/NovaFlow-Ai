import SetupClient from "./SetupClient";

export const metadata = {
  title: "Setup — NovaFlow AI",
  description: "First-run workspace setup",
};

export default function SetupPage() {
  return <SetupClient />;
}
